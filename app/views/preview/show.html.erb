<div class="preview-container">
  <div class="preview" data-id-bar="<%= @bar.id %>"  data-price="<%= @bar.price %>">

    <div class="price prev-bloc">
      <p class="beer-price"><%= transform_price(@bar.price, true) %>€</p>
    </div>
    <div class="main prev-bloc">
      <div class="name-container">
        <h2><%= sanitize(truncate(@bar.name), :length => 30)%></h2>
      </div>
      <p class="distance"></p>

    </div>
    <div class="more-btn prev-bloc">
    </div>
  </div>
</div>

