<div class="bar-form" onload="initialize()">
    <%= form_for(@bar) do |f| %>
      <% if @bar.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@bar.errors.count, "error") %> prohibited this bar from being saved:</h2>

          <ul>
          <% @bar.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field">
        <%= f.text_field :name, placeholder: 'Nom du bar...' %>
      </div>
      <div class="field">
        <%= f.number_field :price, placeholder: 'Prix de la pinte...' %>
      </div>

      <div class="field">
        <h3>Happy Hours ! </h3>

        <span>Début happy</span>
        <%= f.time_select :start_happy%>
        <br>
        <span>Fin happy</span>
        <%= f.time_select :end_happy%>
        <br>
        <%= f.number_field :price_happy, placeholder: 'Prix en Happy Hours...' %>
      </div>
      <h3>Emplacement du bar</h3>
      <div class="field" id="locationField">
        <%= f.text_field :address, placeholder: 'Adresse...', autocomplete: 'off' %>
      </div>
      <div class="actions">
        <%= f.submit %>
      </div>
    <% end %>
</div>


